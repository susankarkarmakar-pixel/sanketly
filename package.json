{
  "name": "sanketly",
  "version": "1.0.0",
  "private": true,
  "description": "A privacy-first, end-to-end encrypted messaging app",
  "scripts": {
    "test": "pnpm -r run test",
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev"
  },
  "dependencies": {
    "@wireapp/proteus": "^9.13.0",
    "libsodium-wrappers-sumo": "^0.8.4"
  }
}
